# Node.js dependencies
# This directory contains all the downloaded packages and can be very large.
# It can be recreated by running `npm install`.
/node_modules

# Build artifacts
# This is the CSS file generated by Tailwind. We only need to track the source file (`input.css`).
/css/style.css


# OS-specific files
# For macOS users who might clone your repository.
.DS_Store
